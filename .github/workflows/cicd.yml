name: CD
on: [push]

jobs:
  CD-develop:
    name: deploy-develop
    runs-on: ubuntu-latest
    #if: ${{ github.ref == 'refs/heads/develop' }}
    steps:
      - name: 리모트 쉘 접속
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            ./repository_pull.sh
#            ./24-not-enough/pm2-run.sh