name: Hello CI
on: [push]
jobs:
  job_test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Checkout Code
      - name: Hello CI
        shell: bash
        run: echo "HELLO!"